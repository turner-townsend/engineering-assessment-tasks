# Copy to .env for local non-Docker runs of the API. All have working defaults.
# SQLite by default; point at Postgres to mirror Docker.
HUB_DATABASE_URL=sqlite:///./local.db
HUB_SEED_ON_STARTUP=true
